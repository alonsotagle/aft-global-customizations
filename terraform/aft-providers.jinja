## Auto generated aft-providers.tf ##
## Updated on: {{ timestamp }} ##

provider "aws" {
  region = "{{ provider_region }}"
  assume_role {
    role_arn = "{{ target_admin_role_arn }}"
  }
  default_tags {
    tags = {
      managed_by = "AFT"
      created_by = "global-customizations"
    }
  }
}

provider "aws" {
  region  = "{{ provider_region }}"
  alias   = "aft_admin_role"
  assume_role {
    role_arn = "{{ aft_admin_role_arn }}"
  }
}
